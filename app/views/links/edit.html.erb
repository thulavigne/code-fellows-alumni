  <% if user_signed_in? %>
    <div class="signed_in">
      <p style="color:#555; line-height:16px; margin:0px;">Signed in as <%= current_user.email %></p>
    </div>
  <% end %>
<div class="container">
<%= link_to "Back", @object %>
<h2>Edit Link</h2>
<%= render "form" %>
</div>
