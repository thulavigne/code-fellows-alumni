<div id="project">
  <h2><%= @project.title %></h2>
  <p>
    <b>Description:</b>
    <%= @project.description %>
  </p>
</div>
<div>
  <% if @project.attach.exists? then %>
    <b>Screenshot</b></br>
    <%= image_tag @project.attach.url %>
  <% end %>
</div>
</br>
 <div class="row">
  <div class="span6">
    <p>
      <b>Project links: </b>
      <% if current_user == @project.user %>
        <%= link_to "Add link", new_project_link_path(@object) %>
      <% end %>
    </p>
    <%= render "links/links_form" %>
  </div>
</div>
