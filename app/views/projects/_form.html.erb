<%= simple_form_for(@project, :html => {:class => 'form-inline'}) do |f| %>
  <%= f.error_notification %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :attach %>
  <%= f.submit %>
<% end %>
