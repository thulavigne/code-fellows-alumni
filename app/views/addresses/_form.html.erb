<%= simple_form_for(@address, :html => {:class => 'form-inline', :id => 'address-form'}) do |f| %>
  <%= f.error_notification %>
  <div class="text-info">Street address and postal code will not be displayed publicly</div>
  <%= f.input :street_address %>
  <%= f.input :city %>
  <%= f.input :state, :include_blank => true,
    prompt: 'Select a country first',
    collection: states,
    value_method: :first %>
  <%= f.input :country, :include_blank => true,
    collection: countries,
    value_method: :last %>
  f.input :postal_code %>
  <%= f.submit %>
<% end %>
